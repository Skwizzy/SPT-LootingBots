{
  "name": "LootingBots",
  "version": "2.0.0",
  "license": "MIT",
  "author": "Skwizzy",
  "description": "Enhanced looting behavior for bots in SPT 4.0",
  "sptVersion": "~4.0.0",
  "scripts": {
      "build": "npm run build:server && npm run build:client",
      "build:server": "cd ./LootingBotsServerMod && dotnet build -c Release",
      "build:client": "cd ./LootingBots && dotnet build -c Release",
      "clean": "npm run clean:server && npm run clean:client",
      "clean:server": "cd ./LootingBotsServerMod && dotnet clean",
      "clean:client": "cd ./LootingBots && dotnet clean"
  },
  "devDependencies": {
      "@types/node": "^16.18.18"
  }
}
